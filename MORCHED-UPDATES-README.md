# ملخص التحديثات — Morched Legale

نسخة معدّلة جاهزة للنسخ فوق مشروعك الحالي (`hm-mo-tn-pf`). فيما يلي كل ما تم تغييره وكل الافتراضات التي
احتجت اتخاذها لعدم توفر بعض المعطيات (روابط، أسعار دقيقة...). عدّلها بسهولة من الأماكن المذكورة أدناه.

## 1) الأسعار (حسب طلبك الأخير)
- تم حذف عرضي 20 د.ت و40 د.ت الشهريين نهائيًا.
- عرض واحد فقط الآن: **94 دينار / سنة** (`lib/pricing.ts` — عدّل `SUBSCRIPTION_PRICE_TND` و
  `SUBSCRIPTION_PRICE_MILLIMES` من هنا فقط، وستنعكس القيمة تلقائيًا في البطل (Hero) وقسم الأسعار).
- تم الإبقاء على **الخطة المؤسسية** (نسخة خاصة/Instance) بصيغة "اتصل بنا" كما كانت.
- الأسعار مضمّنة الآن مباشرة إلى مبلغ Flouci الصحيح (94000 مليم) — في النسخة الأصلية كان المبلغ 20 د.ت
  ثابتًا بغض النظر عن الباقة المختارة؛ تم إصلاح ذلك عبر `handleFlouciStart.bind(null, amount)`.

## 2) الأزرار الثلاثة في البطل (Hero)
1. **تجربة الطلبة – 10 رسائل مجانًا** → نفس تدفق SSO المجاني السابق (يدخل مباشرة workspace "loi").
2. **للمؤسسات – منصة مرشد الكاملة** (جديد) → نفس آلية SSO لكن بدون تحديد workspace واحد، فيهبط
   المستخدم على الصفحة الرئيسية للمنصة (حيث تظهر كل الـ workspaces المتاحة له) — هذا هو تفسيري
   لعبارة "the bigger platform the main morched workspaces". **إن كان المقصود نطاقًا/تطبيقًا مختلفًا
   تمامًا لعملاء المؤسسات**، أضف `NEXT_PUBLIC_ENTERPRISE_URL=https://your-enterprise-domain.tn` في
   `.env` وسيُستعمل تلقائيًا بدل الرابط الافتراضي (`components/hero-section-action.ts`).
3. **94 دينار / سنة عبر Flouci — اشترك** → نفس زر الدفع السابق بالسعر الجديد.

## 3) التنبيه بالعربية فقط (مرئي بوضوح)
- شريط تنبيه أصفر بارز أعلى أزرار البطل مباشرة (`components/hero-section.tsx`) يقول بوضوح إن مرشد
  يفهم العربية فقط (+ أرقام وحروف لاتينية وعلامات ترقيم).
- تسمية صغيرة (`components/widget-language-badge.tsx`) تطفو فوق فقاعة الدردشة مباشرة وتُذكّر بنفس الشيء،
  وتُعرّف الفقاعة كـ"نسخة اختبارية للطلبة".
- **تصفية فعلية للحروف أثناء الكتابة** (`components/chat-input-guard.tsx`): سكربت يعمل في كامل الموقع
  ويحذف أي حرف خارج (عربي + لاتيني + أرقام + علامات ترقيم) فور كتابته، في أي حقل نص بما في ذلك حقل
  ويدجت AnythingLLM إن أمكن الوصول إليه من صفحة المضيف.
  - ⚠️ **حدود تقنية صادقة**: المتصفحات تمنع أي صفحة من "الدخول" داخل `iframe` من نطاق مختلف أو داخل
    `closed shadow DOM` — هذا قيد أمني في كل المتصفحات وليس خطأ في الكود. إن كان ويدجت AnythingLLM
    يُصيَّر بهذه الطريقة، فلن يصل إليه هذا السكربت، والحل الوحيد المضمون 100% هو ضبط الأمر داخل
    system prompt الخاص بـ workspace في AnythingLLM نفسه (مثلاً: "أجب بالعربية فقط، وإن كتب المستخدم
    بلغة أخرى غير العربية/اللاتينية اطلب منه إعادة الكتابة بالعربية"). التنبيهات المرئية (البند أعلاه)
    تبقى فعالة في كل الحالات.

## 4) ويدجت AnythingLLM — العلامة التجارية (`components/anythingllm-widget.tsx`)
تم استعمال الخاصيات الرسمية الموثّقة في
[anythingllm-embed README](https://github.com/Mintplex-Labs/anythingllm-embed/blob/main/README.md):
- `data-brand-image-url` → شعار مرشد (favicon.svg) يظهر أعلى نافذة الدردشة عند فتحها.
- `data-no-sponsor="true"` → يخفي سطر "Powered by AnythingLLM" الافتراضي.
- `data-assistant-name="مرشد القانوني الذكي"` → يستبدل الاسم الافتراضي "AnythingLLM Chat Assistant".
- `data-button-color="#990000"` → لون الفقاعة أحمر مرشد.
- `data-chat-icon="chatBubble"` → **ملاحظة مهمة**: توثيق AnythingLLM الرسمي لا يسمح حاليًا بصورة
  مخصصة (شعارك) لأيقونة الفقاعة المغلقة نفسها — فقط قيم محددة سلفًا
  (`plus, chatBubble, support, search2, search, magic`). لذلك اعتُمد أقرب شكل عام، وتم تعويض ذلك
  بشعار مرشد داخل نافذة الدردشة المفتوحة + التسمية العائمة الصغيرة بجانب الفقاعة.
- تم حذف كل الإشارات إلى "AnythingLLM" و"Mintplex-Labs" من التعليقات الظاهرة في الكود الذي أضفناه —
  ما تبقى هو فقط اسم الدومين التقني (`loi.morched.tn`) الذي أعطيتنا إياه، وهو ضروري لعمل السكربت.

## 5) زر التبرع (`components/donation-button.tsx`)
زر عائم "ادعم مرشد" (أسفل يسار الشاشة) يفتح نافذة منبثقة بتصميم لطيف (Sparkles + Heart) برسالة دافئة
عن "التقنية من أجل الخير" و"التعليم". لا توجد بوابة دفع مخصصة للتبرعات حاليًا، لذا تُوجَّه النافذة
مباشرة إلى واتساب/البريد الإلكتروني (نفس قنوات التواصل الموجودة أصلاً في الموقع). إن توفر رابط دفع
مخصص للتبرعات لاحقًا، أضِفه في متغيّر `DONATION_LINK` أعلى الملف وسيظهر زر دفع مباشر تلقائيًا.

## 6) تموضع العناصر العائمة (لتجنب التداخل)
- فقاعة الدردشة (AnythingLLM): أسفل-يمين (الافتراضي).
- تسمية "نسخة اختبارية للطلبة": أسفل-يمين، أعلى الفقاعة مباشرة (تختفي تلقائيًا بعد 9 ثوانٍ).
- زر الهاتف (في قسم الأسعار فقط): تم رفعه أعلى ليتفادى التداخل مع الفقاعة والتسمية.
- زر التبرع: أسفل-يسار.

## 7) ملفات جديدة
```
lib/pricing.ts                       — سعر الاشتراك الوحيد (مصدر واحد للحقيقة)
components/anythingllm-widget.tsx    — تحميل ويدجت AnythingLLM بعلامة مرشد
components/chat-input-guard.tsx      — تصفية الكتابة (عربي/لاتيني/أرقام/ترقيم فقط)
components/widget-language-badge.tsx — تسمية "نسخة اختبارية للطلبة" + تذكير باللغة
components/donation-button.tsx       — زر ونافذة التبرع
```

## 8) ملفات معدّلة
```
components/hero-section-action.ts  — إضافة handleEnterpriseStart + amount قابل للتمرير لـ Flouci
components/hero-section.tsx        — 3 أزرار + شريط تنبيه اللغة
components/pricing-section.tsx     — باقة واحدة (94 د.ت/سنة) + المؤسسية، رفع زر الهاتف
components/faq-section.tsx         — تحديث إجابة سؤال "خطط الاشتراك"
app/ClientLayout.tsx               — تركيب الويدجت + الحارس اللغوي + زر التبرع
```

## 9) قبل النشر — تحقق من هذه النقاط
- [ ] تأكد أن `NEXT_PUBLIC_ENTERPRISE_URL` (اختياري) يشير للمكان الصحيح إن أردت فصل عملاء المؤسسات
      عن `loi.morched.tn`.
- [ ] تأكد من قيمة `BOTAPI`, `FLOUCI_PUBLIC_KEY`, `FLOUCI_SECRET_KEY`, `NEXT_PUBLIC_BASE_URL` في `.env`
      (لم تتغيّر أسماء هذه المتغيرات).
- [ ] افحص فعليًا (بعد النشر على `legale.morched.tn`) هل يصل سكربت `chat-input-guard.tsx` لحقل الكتابة
      داخل ويدجت AnythingLLM أم لا — إن لم يصل، فعّل القيد داخل system prompt الخاص بـ workspace "loi".
- [ ] إن رغبت بسعر مختلف عن 94 د.ت، عدّل فقط `lib/pricing.ts`.
