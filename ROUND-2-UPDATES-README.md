# تحديثات الجولة الثانية — يرجى القراءة أولًا

## ⚠️ ملفات يجب حذفها يدويًا من مشروعك
لأن أدوات الأرشفة لا تحذف ملفات، احذف هذه الملفات الثلاثة يدويًا (لم تعد مستعملة بعد حذف الويدجت):
```
components/anythingllm-widget.tsx
components/chat-input-guard.tsx
components/widget-language-badge.tsx
```

## ما الذي تغيّر في هذه الجولة

1. **حذف ويدجت الدردشة (AnythingLLM) نهائيًا** — تم حذفه من `app/ClientLayout.tsx` والملفات الثلاثة
   أعلاه. الدردشة الآن تتم فقط عبر أزرار "تجربة الطلبة" و"للمؤسسات" في البطل، التي تُحوّل المستخدم إلى
   منصة `loi.morched.tn` مباشرة.

2. **تبسيط تنبيه اللغة العربية** (`components/hero-section.tsx`) — استبدلنا الصندوق الأصفر الكبير بسطر
   بسيط: **"الدردشة مع مرشد بالأحرف العربية فقط، وليس اللاتينية."**

3. **إصلاح تراكب الأنيميشن خلف النص في الجوال** (`components/hero-section.tsx`) — السبب الحقيقي: عنصر
   الأنيميشن (`particle-text-effect.tsx`) يستعمل `position: absolute; inset: 0` ليملأ أقرب عنصر أب له
   بخاصية `position: relative`. الحاوية المخصصة له لم تكن `relative`، فكان يتمدد ليملأ كامل قسم البطل
   (`<section>`) بدل الصندوق الصغير المخصص له فقط — فيتراكب مع النص الطويل أسفله في الشاشات الصغيرة.
   الإصلاح: أضفنا `relative` على حاوية الأنيميشن مباشرة، فأصبح محصورًا في حدوده الصحيحة فقط.

4. **إصلاح توسيط الشريط العلوي (Header)** (`components/header.tsx`) — كان يعتمد على
   `left-1/2 + transform:-translate-x-1/2` للتوسيط، وهي طريقة يمكن أن تنحرف بضع بكسلات في صفحات RTL
   (خصوصًا مع شريط التمرير الذي يظهر على اليسار في الوضع العربي، مما يغيّر حساب "50% من الشاشة"،
   ويُلاحَظ بشكل أوضح في المتصفح الفعلي). استبدلناها بطريقة أكثر ثباتًا لا تعتمد على حسابات نسبية من
   عرض الشاشة إطلاقًا: `inset-x-0 + mx-auto + w-fit`، وهي الطريقة القياسية لتوسيط عنصر عائم بعرض تلقائي
   بغض النظر عن اتجاه الصفحة أو شريط التمرير.

5. **زر التبرع أصبح فعليًا يدفع عبر Flouci** (`components/donation-button.tsx`):
   - المستخدم يكتب **مبلغًا بالأرقام فقط** (حقل مُقيَّد بالأرقام تلقائيًا) بالدينار التونسي.
   - زر **"تبرع الآن"** يُنشئ عملية دفع Flouci حقيقية (نفس آلية اشتراك 94 د.ت لكن بمبلغ ديناميكي من
     `handleDonationStart(amount)` في `components/hero-section-action.ts`).
   - بعد الدفع، يُعيد Flouci توجيه المتبرع مباشرة إلى **الصفحة الرئيسية لنفس الموقع** (نفس الدومين) عبر
     مسار جديد `app/api/flouci/verify-donation/route.ts` — بخلاف مسار الاشتراك، هذا المسار **لا ينشئ
     حساب دردشة**، فقط يتحقق من نجاح الدفع الحقيقي عبر Flouci API ثم يُعيد التوجيه مع
     `?donation=success` (أو `failed` / `cancelled` / `error`).
   - عند العودة للصفحة الرئيسية، يظهر شريط شكر تلقائي أعلى الصفحة (`DonationThanksBanner` في نفس
     الملف، مُضاف في `app/page.tsx`) يقول: **"شكرًا لدعمك مرشد 💛 ساهمت في دعم الذكاء الاصطناعي من أجل
     التعليم والنمو في تونس."** — يختفي تلقائيًا بعد 7 ثوانٍ.
   - رسالة النافذة المنبثقة قبل الدفع تشرح أن التبرع يدعم "الذكاء الاصطناعي في خدمة التعليم" و"إبقاء
     تجربة الطلبة مجانية" و"النمو" — كما طلبت.

6. **تأكيد**: `NEXT_PUBLIC_ENTERPRISE_URL` يبقى كما هو مُستعملًا في `handleEnterpriseStart` — لم يتغيّر،
   فقط أزلنا شكوك "TODO" من التعليقات لأنك أكّدت أنه يحمل الدومين الصحيح.

## ملفات جديدة
```
app/api/flouci/verify-donation/route.ts   — مسار التحقق من دفع التبرع، يُعيد التوجيه للرئيسية
```

## ملفات معدّلة
```
app/ClientLayout.tsx               — حذف الويدجت من التركيب
app/page.tsx                       — إضافة <DonationThanksBanner />
components/hero-section.tsx        — تنبيه أبسط + إصلاح تراكب الأنيميشن
components/header.tsx              — توسيط أكثر ثباتًا
components/donation-button.tsx     — حقل مبلغ + دفع Flouci حقيقي + شريط شكر
components/hero-section-action.ts  — إضافة handleDonationStart
```

## قبل النشر
- [ ] احذف الملفات الثلاثة المذكورة أعلاه يدويًا.
- [ ] تأكد أن `FLOUCI_PUBLIC_KEY`, `FLOUCI_SECRET_KEY`, `NEXT_PUBLIC_BASE_URL` موجودة في `.env` (نفس
      المتغيرات المستعملة أصلًا لاشتراك الـ94 د.ت — لا حاجة لمتغيرات جديدة).
